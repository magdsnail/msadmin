import { ApiProperty, ApiPropertyOptional } from "@nestjs/swagger";
import { IsNotEmpty, IsOptional, IsString } from "class-validator";
import { RequestDTO } from "../../common/request.dto";

/**
 * 查询岗位列表
 */
export class QueryJob extends RequestDTO {
   
}